name: 'Install packages'
description: 'Install required packages'

runs:
  using: "composite"
  steps:

    - name: Install dependencies
      shell: bash
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        # Aici sunt toate pachetele necesare, inclusiv cele de X11 pentru SFML
        sudo apt-get install -y ninja-build xdotool \
          openbox \
          libxkbcommon-x11-0 \
          libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 libxcb-xinerama0 libxcb-xfixes0 \
          libxrandr-dev libxcursor-dev libxi-dev libx11-dev libgl1-mesa-dev libudev-dev
        
        # https://github.com/llvm/llvm-project/issues/78354
        sudo sysctl vm.mmap_rnd_bits=28

    - name: Install valgrind
      shell: bash
      if: runner.os == 'Linux' && matrix.runs_valgrind == true
      run: |
        sudo apt-get install --no-install-recommends valgrind